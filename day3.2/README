今天的内容主要是研究，利用int0x13读取u盘，软盘后面扇区的过程
读取u盘的18个扇区到内存,读取出10个柱面
**************************************************************
读取180kB的u盘内容到内存，内存地址空间从0x8000开始，
如果读取u盘数据到内存成功，就会显示last sector read ok，然后进入到loop

在把boot.out文件写入到u盘中时，需要用hexedit把0x2d000-1处的值改为0xaa,
用于检测u盘读取是否成功的标记。
**************************************************************

总的大小是chs=0,0,1 到chs=0,5,63
所以扇区数为6x63=378sectors个扇区读到了内存中，起使地址是0x8000
